module.exports = class Graph {
    constructor(options) {
        this.nodes = new Map()
    }

    addNode(id, node) {
        node.data = node
        node.id = node
        node.links = 

        this.nodes.add(node, node)
    }

    addLink(a, b) {

    }
} 